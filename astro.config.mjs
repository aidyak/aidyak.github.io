// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://aidyak.github.io',
  // For user pages (aidyak.github.io), base should be root
  base: '/',
});
